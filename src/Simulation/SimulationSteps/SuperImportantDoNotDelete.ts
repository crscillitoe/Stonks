import { StockMap } from "../../main";
import { PositionMap } from "../MyCrankChristianDidn'tHelp";

export interface TheTypeOfThisClass {
  foo?: string;
  bar?: number;
  Z?: boolean;
  positionMap: PositionMap;
  stockMap: StockMap;
}
